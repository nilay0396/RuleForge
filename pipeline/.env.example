# Required: Your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the default Claude model
CLAUDE_MODEL=claude-sonnet-4-6

# Optional: data directory (defaults to pipeline/data)
# DATA_DIR=/path/to/data
